
type Query {
  users: [User],
  showUser: User,
  userRooms: [Room],
  roomMessages(room_id: ID!, page: Int): PaginatedMessage,
  viewImage(id: ID!): String,
  getConsumer(access_key: String!): Consumer
}